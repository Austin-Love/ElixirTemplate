# Environment variables for both Windows and Mac/Linux
# This file is parsed as simple KEY=VALUE pairs, not as a shell script

# For Mac/Linux, uncomment this line:
# USERPROFILE=$HOME

# For Windows, this is already set by the system
# No action needed